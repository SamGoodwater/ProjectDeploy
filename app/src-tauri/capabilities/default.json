{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "com.projectdeploy.app",
  "description": "Capabilities par défaut",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:event:allow-emit",
    "core:event:allow-listen",
    "shell:allow-open"
  ]
}
